{
  "name": "yf-pos",
  "version": "0.0.0",
  "private": true,
  "description": "Yun Fook Trading POS — offline-first desktop till (Vue 3 SPA, wrapper-agnostic)",
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "engines": { "node": ">=22" },
  "scripts": {
    "dev": "pnpm --filter @yf/pos dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["electron", "better-sqlite3", "esbuild"]
  }
}
